let Discord = require('discord.js');

exports.run = (client, message) => {
	
  message.reply(`Total Bans** ${fetchBans}`)
	
message.react('😂');
	
}
module.exports.help = {
	name: "bans",
  alias: "i bans"
}
	
